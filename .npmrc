# Security: disable lifecycle scripts by default to prevent supply chain attacks.
# Packages that legitimately need postinstall (e.g., prisma, sharp) are
# explicitly allowed below.
ignore-scripts=true
